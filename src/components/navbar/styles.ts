import styled from 'styled-components';

export const StyledNavbar: any = styled.nav`
  display: flex;
  border-bottom: 1px solid tomato;
  margin: 0 10px;
`;

StyledNavbar.Link = styled.a`
  font-size: 2rem;
  margin-right: 12px;
  cursor: pointer;
`;
